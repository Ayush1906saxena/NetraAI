name: netra_capture
description: Netra AI Fundus Capture App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0
  dio: ^5.7.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image_picker: ^1.1.0
  path_provider: ^2.1.0
  permission_handler: ^11.3.0
  connectivity_plus: ^6.0.0
  go_router: ^14.0.0
  json_annotation: ^4.9.0
  flutter_secure_storage: ^9.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.0
  json_serializable: ^6.8.0

flutter:
  uses-material-design: true
